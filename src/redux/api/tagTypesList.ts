export enum tagTypes {
    user = "user",
    info = "info",
    aiConfig = "aiConfig",
}

export const tagTypesList = Object.values(tagTypes);

export const METHOD = {
    POST: "POST",
    GET: "GET",
    PUT: "PUT",
    PATCH: "PATCH",
    DELETE: "DELETE",
};
